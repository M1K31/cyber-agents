# Generated / runtime
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# OS / editor
.DS_Store
._*
.obsidian/
*.swp

# Local runtime data (the daemon writes to ~/.aegissiem-daemon, not the repo)
*.log
*.db
*.db-shm
*.db-wal

# --- Documentation policy ---------------------------------------------------
# Only user-facing docs are tracked: README, usage guides, API/user references,
# and changelogs. Agent-generated planning artifacts (plans, specs, findings,
# audits, walkthroughs) stay local and are added only after an explicit
# document review.
docs/superpowers/
*_PLAN.md
*_FINDINGS.md
*_AUDIT.md
AUDIT_*.md
walkthrough.md
WALKTHROUGH.md
